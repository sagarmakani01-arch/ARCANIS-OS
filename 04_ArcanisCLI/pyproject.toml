[project]
name = "arcanis-cli"
version = "0.6.0"
description = "Unified Arcanis CLI"
requires-python = ">=3.11"
license = { text = "Proprietary" }

[project.scripts]
arcanis = "arcanis_cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends._legacy:_Backend"
